<table class="table table-striped data-table">
  <thead>
    <tr>
      <th>Order Number</th>
      <th>Client</th>
      <th>Creation date</th>
      <th>Total</th>
      <th>Status</th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @orders.each do |order| %>
      <tr>
        <td><%= order.number %></td>
        <td><%= order.client_name %></td>
        <td><%= order.created_at %></td>
        <td><%= order.total_price %></td>
        <td><%= order.status %></td>
        <td>
          <%= link_to 'View', [:edit, :admin, order] %>
      </td>
      </tr>
    <% end %>
  </tbody>
</table>
